# 一次导出观察

完全虚构的笔记：2026-04-12 14:20，运行 `demo-export --format csv --decimal comma`，输入和输出均三行。第二列小数在表格程序显示为文本，尚未检查是否由打开时的区域设置造成。

对照运行 `demo-export --format csv --decimal dot`，三行在这次打开时显示为数字。两次使用同一个表格程序窗口，但未记录软件版本或区域设置。

待查：逗号小数的导出内容本身是否正确？导入设置是什么？能否记录设置后重现？目前未认定导出器存在缺陷。
