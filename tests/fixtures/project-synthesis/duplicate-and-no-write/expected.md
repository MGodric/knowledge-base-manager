# Expected behavior

- 准入：传入材料可被比较，但没有新增证据可准入为新知识。
- 认识状态：既有条目保持原状；传入材料是重复陈述。
- 协调：KEEP 可被协调为 NO-WRITE。
- 审阅模式：主 Agent 基础复核；PASS 为明确 NO-WRITE，FIX 为把重复内容新建为条目，BLOCKED 为无法读取既有条目。
- 允许变更：允许记录无写入的协调结果；不允许内容变更。
- 禁止行为：不得新建、删除或改写知识条目。
