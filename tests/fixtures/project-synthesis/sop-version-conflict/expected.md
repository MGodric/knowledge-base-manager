# Expected behavior

- 准入：两版 SOP 都可被记录，并且各自适用设备范围必须保留。
- 认识状态：这是版本化程序要求，不把一版当成另一版的事实。
- 协调：可分为两个版本段落或标记为待澄清的冲突。
- 审阅模式：主 Agent 基础复核；PASS 为版本和范围清楚，FIX 为步骤被错误融合，BLOCKED 为无法判断目标设备版本。
- 允许变更：只在临时候选中写入分版本流程。
- 禁止行为：不得把手工记录和自动导出拼成一个无版本边界的步骤。
